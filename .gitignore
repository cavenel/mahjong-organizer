# Credentials — never commit
certbot/ovh.ini
.env
scripts/backup_db.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Django
staticfiles/
*.sqlite3

# Docker
*.sock

# Pytest
.pytest_cache/

# Claude Code local settings
.claude/settings.local.json

# Backup and temp folders
bakup/
tmp/

# Local static export of a tournament (e.g. `export_public --out ./old-results`)
old-results/

# Score-sheet scan debug crops (runtime output)
captures/

# Node/npm (if used)
node_modules/
package-lock.json

# Local-only load-test / query-profiling tooling (not for the repo or image)
locustfile.py
test_queries.sh
test_queries_2.sh

# Generated at build time by `manage.py build_docs_pdf` (collected into staticfiles)
mahj/static/docs/

# Screenshot pipeline scratch (scripts/screenshots/README.md)
scripts/screenshots/.local/
scripts/screenshots/.pixi/
scripts/screenshots/pixi.lock
