# 5.2 LTS: security-supported for years, where 4.2's extended support ended in
# April 2026. Deliberately not the newest release — 6.x is non-LTS, so it would
# mean another bump within about a year for no feature this app uses.
Django==5.2.17

# Async/WebSocket stack — pinned to a combination verified against the Redis
# channel layer. While unpinned, builds resolved a redis-py whose blocking read
# raised "Timeout reading from redis" and tore down every WebSocket a few
# seconds after connect (display screens reloaded in a loop). redis-py is pulled
# transitively by both django-redis and channels-redis, so pin it explicitly.
channels==4.3.2
channels-redis==4.3.0
redis==7.4.0

psycopg2-binary==2.9.12
django-redis==6.0.0
django-sesame==3.2.3
django-mathfilters==1.0.0
openpyxl==3.1.5
pycountry==26.2.16
Unidecode==1.4.0
whitenoise==6.12.0
opencv-python-headless==4.13.0.92
Pillow==12.2.0
numpy==2.4.6
requests==2.34.2
anthropic==0.111.0
segno==1.6.6

# SFTP upload of the exported public static site (mahj/publish/sftp_upload.py).
paramiko==3.5.1
